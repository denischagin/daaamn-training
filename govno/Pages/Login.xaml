<Page x:Class="govno.Pages.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:govno.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Login">

    <Grid>
        <StackPanel>
            <TextBlock Text="Войти" FontSize="24" />
            <Label Content="Логин" Margin="0 30 0 0" />
            <TextBox TextWrapping="Wrap" Text="admin" x:Name="login" />

            <Label Content="Пароль" Margin="0 10 0 0" />
            <PasswordBox x:Name="password"  />

            <Button Content="Войти" Margin="0 10 0 0" Click="Login_Click" />
            <Button Content="Нет аккаунта?" Margin="0 10 0 0" Background="Transparent" BorderThickness="0" Foreground="Blue" Click="Registration_Click" />
        </StackPanel>

    </Grid>
</Page>
